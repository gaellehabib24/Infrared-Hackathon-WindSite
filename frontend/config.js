// Backend URL — update this after deploying to Render.
// Local dev: leave as empty string (browser calls same-origin localhost:8000).
// Production: paste your Render service URL, e.g.:
//   window.BACKEND_URL = 'https://windsite-backend.onrender.com';
window.BACKEND_URL = '';
